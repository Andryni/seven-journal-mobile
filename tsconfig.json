{
  "extends": "expo/tsconfig.base",
  "compilerOptions": {
    "strict": true
  },
  // supabase/functions runs on Deno, not React Native: it has its own globals
  // and is type-checked by the Supabase CLI, not by the app build.
  "exclude": ["node_modules", "src/**/__tests__", "supabase"]
}
